package com.example.movio.core.common

/**
 * Marker interface to mark instances to be of type Data.
 *
 * Necessary for the ViewModels type parameters. As each ViewModel needs to receive instances of type Data
 *
 * TODO replace marker interfaces with annotations.
 * */
interface Data {
}